"use client";

import ReportTab from "@/src/sections/reportsSections/ReportTab";

// Identical tab-pill pattern to the Reports page — ReportTab is already the
// generic icon+label pill, so this is a thin semantic re-export rather than
// a duplicate implementation.
const SettingsTab = ReportTab;

export default SettingsTab;
